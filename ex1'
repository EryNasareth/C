#include <stdio.h>

int maior(int x1, int x2) {
    if (x1 > x2)
        return x1;
    else
        return x2;
}
int main() {
    int x1, x2;
    scanf("%d %d", &x1, &x2);
    int M = maior(x1, x2);
    printf("%d", M);
    return 0;
}
